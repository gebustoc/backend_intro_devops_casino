apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pg-static
  namespace: default
spec:
  volumeName: pg-static
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 4Gi
