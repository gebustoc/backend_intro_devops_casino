apiVersion: apps/v1
kind: Deployment
metadata:
  name: casino-back
  labels:
    app: casino-back
spec:
  replicas: 2
  selector:
    matchLabels:
      app: casino-back
  template:
    metadata:
      labels:
        app: casino-back
    spec:
      containers:
        - name: casino-back
          image: <ACCOUNT_ID>.dkr.ecr.<REGION>.amazonaws.com/casino-back:latest
          ports:
            - containerPort: 3000
          # Probe de "vivo": si falla, Kubernetes reinicia el contenedor.
          livenessProbe:
            httpGet:
              path: /livez
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
          # Probe de "listo": si falla, no recibe tráfico desde el Service.
          readinessProbe:
            httpGet:
              path: /readyz
              port: 3000
            initialDelaySeconds: 3
            periodSeconds: 5
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "250m"
              memory: "128Mi"


          env:
            - name: DB_HOST
              value: "database"
            - name: DB_USER
              value: "casino" 
            - name: DB_PASSWORD
              value: "casino" 
            - name: DB_NAME
              value: "casino_db"
            - name: JWT_SECRET
              value: "dev"
            - name: CORS_ORIGIN
              value: "*"